{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/((?!assets/|favicon.ico|manifest.webmanifest).*)",
      "destination": "/index.html"
    }
  ]
}
